#### instana operator install

Instana operator is installed with the `kubectl plugin`.

operator namespace
image pull secrets
tls secrets for admission webhook
values file
apply manifests
options configuration
